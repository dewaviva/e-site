[build]
  command = "npm run build"
  publish = "dist/client"

[dev]
  command = "npm run dev"
  targetPort = 3000
  port = 8888

# Redirect semua route ke index.html untuk SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200